from setuptools import setup

setup(
    name="rir-model-eval",
    version="1.0",
    description="RIR model evaluation",
    author="Kyung Yun Lee",
    author_email="foomail@foo.example",
    packages=["models", "datasets", "rir_model_eval"],
    install_requires=[],
)
